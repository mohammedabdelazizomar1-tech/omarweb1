@model BusinessManagement.Application.DTOs.RevenueReportDto
@{
    Layout = "_Layout";
    ViewData["Title"] = "تقرير الإيرادات والمبيعات";
    string fromDate = ViewBag.FromDate;
    string toDate = ViewBag.ToDate;
}

<div class="container-fluid py-4 text-right">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4 border-bottom border-secondary pb-3">
        <div>
            <h4 class="text-white fw-bold m-0"><i class="bi bi-plus-circle-fill text-success me-2"></i> تقرير إيرادات النشاط التشغيلي والمبيعات</h4>
            <p class="text-muted small m-0">رصد وتحليل مصادر الدخل التشغيلي والخدمات السياحية المباعة خلال فترة محاسبية معينة.</p>
        </div>
        <div class="d-flex gap-2 no-print">
            <a href="/Reports/ExportRevenueExcel?fromDate=@fromDate&toDate=@toDate" class="btn btn-outline-success btn-sm"><i class="bi bi-file-earmark-excel"></i> تصدير Excel</a>
            <button onclick="window.print();" class="btn btn-outline-info btn-sm"><i class="bi bi-printer"></i> طباعة / PDF</button>
            <a href="/Reports" class="btn btn-outline-secondary btn-sm">العودة</a>
        </div>
    </div>

    <!-- Filters -->
    <div class="glass-card p-3 mb-4 no-print">
        <form method="get" class="row g-3 align-items-end">
            <div class="col-md-5">
                <label class="form-label text-secondary small">من تاريخ</label>
                <input type="date" name="fromDate" value="@fromDate" class="form-control" />
            </div>
            <div class="col-md-5">
                <label class="form-label text-secondary small">إلى تاريخ</label>
                <input type="date" name="toDate" value="@toDate" class="form-control" />
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-100"><i class="bi bi-funnel"></i> تصفية</button>
            </div>
        </form>
    </div>

    <!-- Summary Stats -->
    <div class="row g-3 mb-4">
        <div class="col-md-6">
            <div class="glass-card p-4 border-start border-success border-4">
                <small class="text-muted">إجمالي الإيرادات الكلية للفترة</small>
                <h2 class="text-success fw-bold m-0 mt-1">@Model.TotalRevenues.ToString("N2") ج.م</h2>
            </div>
        </div>
    </div>

    <!-- Breakdown Table -->
    <div class="glass-card p-4">
        <h6 class="text-white fw-bold mb-3 border-bottom border-secondary pb-2">تفصيل مصادر الإيرادات وتصنيفها المالي</h6>
        <div class="table-responsive">
            <table class="BusinessManagement-table">
                <thead>
                    <tr>
                        <th>كود الحساب</th>
                        <th>فئة الإيراد / الخدمة</th>
                        <th class="text-end">مبلغ المبيعات</th>
                        <th class="text-end">النسبة المساهمة من إجمالي الإيراد</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.RevenuesByCategory.Any())
                    {
                        <tr>
                            <td colspan="4" class="text-center text-muted py-4">لم تسجل أي بنود إيراد للفترة المحددة.</td>
                        </tr>
                    }
                    else
                    {
                        foreach (var r in Model.RevenuesByCategory)
                        {
                            <tr>
                                <td class="text-muted font-monospace">@r.AccountCode</td>
                                <td class="fw-bold text-white">@r.CategoryName</td>
                                <td class="text-end text-success font-monospace">@r.Amount.ToString("N2") ج.م</td>
                                <td class="text-end">
                                    <div class="d-flex align-items-center justify-content-end gap-2">
                                        <span class="text-white fw-bold">@r.Percentage.ToString("F1")%</span>
                                        <div class="progress w-25" style="height: 6px; background-color: rgba(255,255,255,0.05);">
                                            <div class="progress-bar bg-success" role="progressbar" style="width: @(r.Percentage)%;"></div>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
